name: imamsu1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔹 LOCAL DATABASE (HIVE)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # 🔹 WHATSAPP REDIRECT
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # 🔹 HIVE CODE GENERATOR
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
